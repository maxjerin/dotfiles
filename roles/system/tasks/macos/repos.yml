---
# Repository installation is Linux-only (yum_repository)
# This file is included for consistency but does nothing on macOS
- name: Skip repository installation on macOS
  ansible.builtin.debug:
    msg: "Repository installation is Linux-only and skipped on macOS"
  tags:
    - macos
    - macos_repos
